services:
  postgres:
    image: postgres:16
    container_name: saber-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: saber_development
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
